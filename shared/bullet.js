export default function Bullet(id, x, y, dx, dy, shotBy) {
    this.id =  id
    this.x = x
    this.y = y
    this.radius = 10
    this.dx = dx
    this.dy = dy
    this.shotBy = shotBy
    this.ttl = 2

}
